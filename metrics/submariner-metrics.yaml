# Overhead due to submariner in the entire namespace (gateway + lighthouse + metrics + operator + routeagents)
sum(irate(container_cpu_usage_seconds_total{namespace="submariner-operator",container!=""}[2m]) * 100) by (namespace)
